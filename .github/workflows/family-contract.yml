# 러닝봄 생성물·PWA·분석 금지 필드를 immutable 패밀리 정본으로 검증한다.
name: Family contract

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  family-contract:
    uses: robom-labs/robom/.github/workflows/family-contract.yml@5a63eab4f6930837f9877efd187562b85530a81a
    with:
      generated-dir: generated/robom-family
      lock-file: family.lock.json
      manifest-file: outputs/pushrun-site/manifest.webmanifest
